# 환경 변수 예제 파일
# 실제 사용 시 .env 파일로 복사하여 사용하세요
# cp .env.example .env

# [신입 개발자를 위한 설명]
# 환경 변수는 애플리케이션의 설정을 코드 외부에서 관리하는 방법입니다.
# 보안이 중요한 정보(API 키, 비밀번호)는 절대 코드에 직접 작성하지 마세요.

# 노드 환경 (development, production, test)
NODE_ENV=development

# [Vite 환경 변수]
# Vite에서는 VITE_ 접두사가 붙은 변수만 클라이언트에서 접근 가능합니다.
# 보안이 중요한 변수에는 VITE_ 접두사를 붙이지 마세요!

# 공개 API URL (클라이언트에서 접근 가능)
VITE_PUBLIC_API_URL=http://localhost:3000/api

# 애플리케이션 버전
VITE_APP_VERSION=1.0.0

# [서버 전용 환경 변수]
# 아래 변수들은 서버에서만 접근 가능하며 클라이언트로 노출되지 않습니다.

# 데이터베이스 URL
DATABASE_URL=postgresql://user:password@localhost:5432/mydb

# JWT 비밀 키 (실제 프로덕션에서는 강력한 랜덤 문자열 사용)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# 세션 비밀 키
SESSION_SECRET=your-super-secret-session-key-change-this-in-production

# API 키 (서버에서만 사용)
EXTERNAL_API_KEY=your-api-key-here

# 이메일 설정
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-email-password

# 파일 업로드 경로
UPLOAD_DIR=./uploads

# 최대 파일 크기 (바이트)
MAX_FILE_SIZE=5242880

# [개발 도구 설정]
# Vite 개발 서버 포트
PORT=3000

# Vite 개발 서버 호스트
HOST=localhost
